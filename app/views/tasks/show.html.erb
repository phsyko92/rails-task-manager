<h2><%= @task.title %></h2>
<p><%= @task.details %></p>


<% if @task.completed == true %>
  <p>task is done</p>
<% end %>
<%= link_to "retour vers l'index", tasks_path %>
<%= link_to "edit cette task", edit_task_path(@task) %>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
